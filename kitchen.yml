---
driver:
  name: vagrant
  synced_folders:
   - ['.', '/tmp/habitat']

provisioner:
  name: shell

platforms:
  - name: centos-7
  - name: centos-8

verifier:
  name: inspec

suites:
  - name: opbldr
    verifier:
      inspec_tests:
        - path: test/integration/default
    attributes:
      build: false
